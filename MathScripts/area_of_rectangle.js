"use strict";

// Declare known values.

var width = 3;
var length = 5;

// Calculations.

var area = width * length;

// Display the results.

var message = "The area of a rectangle is " + area;

console.log(message);